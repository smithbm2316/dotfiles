require('mini.splitjoin').setup {
  -- Module mappings. Use `''` (empty string) to disable one. Created for both Normal and Visual modes.
  mappings = {
    toggle = 'gJ',
    split = '',
    join = '',
  },
}
